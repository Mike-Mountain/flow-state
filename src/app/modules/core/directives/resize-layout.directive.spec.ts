import { ResizeLayoutDirective } from './resize-layout.directive';

describe('ResizeLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeLayoutDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
